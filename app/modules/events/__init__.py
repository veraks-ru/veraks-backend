"""Домен events: редакционные события (прогнозируемые исходы).

Слои (порты-и-адаптеры):
    domain/       — сущности Event/Category, конечный автомат статусов,
                    политики (без I/O);
    ports/        — абстрактные интерфейсы (репозитории, часы);
    application/  — use-cases CRUD и переходов жизненного цикла;
    adapters/     — реализации портов (SQLAlchemy, системные часы);
    api/          — FastAPI-роутер, схемы, DI-обвязка.

Скоринг, разрешения (resolutions) и оспаривания (disputes) — отдельные
домены; переходы статусов, инициируемые ими, помечены ``TODO``.
"""
