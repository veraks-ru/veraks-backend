"""Домен identity: аутентификация через ЕСИА и управление аккаунтами.

Слои (порты-и-адаптеры):
    domain/       — чистые сущности, value-objects, политики (без I/O);
    ports/        — абстрактные интерфейсы (репозитории, шлюз ЕСИА, крипто);
    application/  — use-cases, оркеструющие порты;
    adapters/     — реализации портов (SQLAlchemy, OIDC, JWT, Redis);
    api/          — FastAPI-роутер, схемы, DI-обвязка.
"""
