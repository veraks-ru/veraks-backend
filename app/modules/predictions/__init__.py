"""Домен predictions: приём прогнозов как градаций уверенности.

Пользователь выбирает градацию уверенности (``confidence_grade``), которая
детерминированно отображается во внутреннюю вероятность (``probability``).
Прогноз редактируем до блокировки по серверному ``closes_at`` события; после
дедлайна правки запрещены. История изменений фиксируется через порт аудита
(см. ``ports/audit.py``), а сам прогноз хранится latest-wins одной строкой.
"""
